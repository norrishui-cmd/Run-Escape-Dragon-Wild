{
  "name": "nivalis-nights-wiki",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-pages.mjs && node scripts/generate-i18n.mjs && node scripts/configure-adsense.mjs",
    "audit": "node scripts/seo-audit.mjs",
    "build": "npm run generate && npm run audit"
  }
}
